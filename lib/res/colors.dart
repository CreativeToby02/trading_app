import 'package:flutter/cupertino.dart';

@immutable
class AppColors {
  static const primaryColor = Color(0xFF1E3741);
  static const secondaryColor = Color(0xFF14262E);
  static const whiteColor = Color(0xFFFFFFFF);
  static const accentColor = Color(0xFF2991BB);
  static const highlightColor = Color(0xFF00D8C7);
  static const blackColor = Color(0xFF000000);

  const AppColors._(); // Private constructor to prevent instantiation
}
